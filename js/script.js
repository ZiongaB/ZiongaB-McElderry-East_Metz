

// var filterMenu = document.getElementById("allergyFilterWithDropdown-Container");



// break this into seperate functions, finalize with 
// function transformFilterButton(el){
//     alert("I work!");
    function toggle(el){
        var filterMenu = document.getElementById("allergyFilterWithDropdown-Container");
        filterMenu.style.display = tag.style.display === 'block' ? 'none' : 'block';
    }

var filterButtonOption = document.getElementByClassName(filterSelector);
function recolor() {
    filterButtonOption;
    
}

// if the window height or width does not equal the maximum width and maximum height, 
// then adjust the maximum width and maximum height to the current window sizes
if (condition) {
}